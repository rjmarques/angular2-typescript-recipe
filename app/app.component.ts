import { Component } from '@angular/core';

@Component({
  selector: 'recipe-app',
  template: '<h1>Yay the recipe works! Now onto more instesting stuff. Good luck!</h1>'
})

class Hero {
  name: string;
}

var h: Hero = {name: "Hi"};

export class AppComponent { }
